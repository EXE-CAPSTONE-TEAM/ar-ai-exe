{
  "name": "kusshoes-editor-desktop",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "build:frontend": "npm --prefix ../frontend run build -- --mode desktop",
    "build:sidecar": "powershell -ExecutionPolicy Bypass -File .\\scripts\\build-backend-sidecar.ps1",
    "prepare:blender": "powershell -ExecutionPolicy Bypass -File .\\scripts\\prepare-blender-runtime.ps1",
    "build:production": "powershell -ExecutionPolicy Bypass -File .\\scripts\\build-production-desktop.ps1"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.2"
  }
}
